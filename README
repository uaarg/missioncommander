requirements
sudo apt-get install python-pip
sudo pip install pyproj requests


set paparazzi path
sudo gedit /etc/environment
```
PAPARAZZI_SRC="/home/rijesh/paparazzi"
PAPARAZZI_HOME="/home/rijesh/paparazzi"

```



Setting up Interop Server
References:
https://github.com/auvsi-suas/interop
https://auvsi-suas-competition-interoperability-system.readthedocs.io/en/latest/

git clone https://github.com/auvsi-suas/interop.git
cd ~/interop
sudo ./tools/setup_docker.sh ./tools/setup_tools.sh
sudo ./server/run.sh

start and stop
sudo docker stop interop-server
sudo docker start interop-server


running mission commander
Turn on a paparazzi and choose Microjet under A/C.
This configuration has the lexington park flight plan enabled by default and is garunteed to work.

go to mission commander and run via:
python missioncommander
